import { injectGlobal } from 'styled-components';
import 'sanitize.css';

export const theme = {
  menuLeft: {
    default: '#333333',
    white: '#fff',
  },
  header: {
    default: '#fff',
  },
  container: {
    maxWidth: '1024px',
    gutter: '20px',
  },
};

injectGlobal`

`;
